* verify when qmail-remote is called with multiple recipients, to
  prevent problems:

   - revealing of BCC in X-hashcash headers

   - perhaps somehow leading to double spend of tokens

* Is it OK that the signature is always the same when using the same
  key and mail?

